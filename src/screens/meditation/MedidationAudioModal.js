import React, {Component} from 'react';
import {View, Text, Image} from 'react-native';
import AudioModal from '../../components/AudioModal';

export default class MedidationAudioModal extends Component {
    render() {
        return (
            <View>
                <AudioModal/>
            </View>
        )
    }
}